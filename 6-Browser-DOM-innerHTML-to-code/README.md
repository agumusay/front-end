Get the innerHTML of the input section.
Next, print it to the output section.

Make sure the innerHTML appears as code not as rendered HTML in the output section.

Hint: use `.replace(/</g,"&lt;")` to achieve this.